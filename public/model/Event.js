export class Event {
    uid = null; //generated by firebase
    title = null;
    description = null;
    category = null;
    start = null; //date
    finish = null; //date
    reminderBool = null; //boolean
    reminderTime = null; //when
    docId = null;

    constructor(data) {
        this.uid = data.uid
        this.title = data.title;
        this.description = data.description;
        this.category = data.category;
        this.start = data.start;
        this.finish = data.finish;
        this.reminderBool = data.reminderBool;
        this.reminderTime = data.reminderTime;
    }

    set_docId(docId) {
        this.docId = docId;
    }

    //this function converts the data of the model into something the firestore database understands /can store
    toFirestore() {
        return {
            uid: this.uid,
            title: this.title,
            description: this.description,
            category: this.category,
            start: this.start,
            finish: this.finish,
            reminderBool: this.reminderBool,
            reminderTime: this.reminderTime
        };
    }
}